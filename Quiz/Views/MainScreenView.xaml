<UserControl x:Class="Quiz.Views.MainScreenView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	mc:Ignorable="d"
	d:DesignHeight="300"
	d:DesignWidth="300">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="*" />
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="50" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<Button Grid.Row="0"
			Grid.Column="0"
			x:Name="StartSecondLevelQuiz"
			Content="Segundo ciclo!"
			Margin="5" />

		<Button Grid.Row="0"
			Grid.Column="1"
			x:Name="StartThirdLevelQuiz"
			Content="Terceiro ciclo!"
			Margin="5" />

        <ContentControl x:Name="SecondLevelHighScoreViewModel"
			Grid.Row="1"
			Grid.Column="0" />

		<ContentControl x:Name="ThirdLevelHighScoreViewModel"
			Grid.Row="1"
			Grid.Column="1" />

	</Grid>
</UserControl>